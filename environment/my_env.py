import os
from uiautomator import Device

# abandoned
# not used
class qt_Environment():
    def __init__(self):
        print()

    def reset(self):
        cmd = 'return to the beginning'
        cmd = ''
        os.system(cmd)
        state_key = 1
        return state_key
